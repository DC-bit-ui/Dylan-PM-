---
description: Route a task to the right subagent (or set of subagents in parallel).
argument-hint: <task description>
---

Invoke the `delegate` skill with: $ARGUMENTS

Decompose the task, dispatch agents in parallel where strands are independent, and synthesize results back into a single response.
