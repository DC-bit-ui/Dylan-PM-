---
description: Log a decision in ADR style.
argument-hint: <short title — what was decided>
---

Invoke the `decision-log` skill with: $ARGUMENTS

If $ARGUMENTS is empty, ask Dylan three short questions: title, context, alternatives considered.
