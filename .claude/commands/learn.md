---
description: Capture a learning Dylan just shared. Routes it to the right memory file and updates indexes.
argument-hint: <the thing to remember, in plain language>
---

Invoke the `log-learning` skill with the following input:

$ARGUMENTS

If `$ARGUMENTS` is empty, ask Dylan one short question: *"What did you just learn / what should I remember?"* — then proceed.

After filing, briefly confirm:
- What was captured (one sentence)
- Where it was filed (path)
- Whether it superseded or links to anything existing
