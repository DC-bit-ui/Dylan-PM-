# INDEX abolished (2026-07-16)

Hand-maintained learning indexes drifted universally (audit defect D3.5) and were retired in the OS rebuild. **The directory listing is the index** — files are named `YYYY-MM-DD-<slug>.md`. Behavioural rules extracted from this corpus live in `memory/state/rules.md`. Policy: `core/PROTOCOLS.md` §Naming.
