# Integrations — guide (hand-maintained INDEX abolished 2026-07-16)

One contract file per connector in this directory (notion, jira, granola, teams, outlook, hubspot, confluence, cowork, external-skills) — the directory listing is the index. ⚠️ Known defect being corrected incrementally: some contracts cite outdated MCP tool-ID prefixes — tool IDs are instance-specific; trust tool NAMES (e.g. `searchJiraIssuesUsingJql`, `read_resource`) and discover the live prefix in-session. The canonical scheduled-prompt store is `.claude/skills/cowork-scheduled/` — `cowork/` here holds HISTORICAL snapshots only.
