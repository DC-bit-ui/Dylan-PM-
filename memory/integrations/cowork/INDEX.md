# HISTORICAL — Apex prompt snapshots (do not treat as current)

**As of 2026-05-20 the canonical store for every scheduled-task prompt is `.claude/skills/cowork-scheduled/<task>/SKILL.md`.** Files here are dated historical snapshots kept as a diff trail; several are older than the canonical copies (the 2026-05-20 morning snapshot predates the read_resource Teams fix). The previous "Current" label on the 2026-05-20 morning snapshot was wrong and is withdrawn (2026-07-16).

Note: `apex-ai-pulse-weekly`, `apex-ai-pulse-breaking`, and `apex-memory-curator` prompts exist ONLY here, were never canonicalised, and depend on `apex-pm/ai-pulse/source_trust.json` which does not exist — **retired 2026-07-16** with the legacy Apex machinery (see `memory/decisions/2026-07-16-os-rebuild.md` §7).

Exception: `apex-data-sources.md` in this directory is NOT a snapshot — it is the live Teams-channel inventory + source-tier contract, and stays authoritative.
