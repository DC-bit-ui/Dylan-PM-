# workspace/archive/

> Completed work from `workspace/current/`, kept for retrieval. Don't reach for archive often — durable artifacts should live in `memory/deliverables/` instead. Use this for ephemeral scratch / standups / focus picks that have aged out.

## Convention
- Keep one folder per month: `YYYY-MM/`
- Move stale items here, don't delete
