# workspace/ — RETIRED (2026-07-16)

Retired in the OS rebuild. Session scratch belongs in the session itself; durable outputs go to `memory/deliverables/<subdir>/`; the old handoff doc moved to `memory/deliverables/handoffs/`. See `core/MAP.md`.
