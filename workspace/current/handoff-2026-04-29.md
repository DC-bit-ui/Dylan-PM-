# Handoff — Cowork OS Implementation, 2026-04-29

> **For:** Local Claude Code instance running at `C:\Dylan PM` on Windows.
> **Authored by:** Sandboxed Claude Code session (paste-relay surface), 2026-04-29.
> **Reason:** Cowork OS bootstrap and consolidation work needs local execution (host shell, git, file system). The sandboxed session can't reach Dylan's Windows filesystem; you can.

---

## You are picking up cold — read this first

You are running on Dylan's Windows machine at `C:\Dylan PM`. You have:
- Full local filesystem access
- PowerShell + git on the host
- The same `memory/` tree as the canonical remote (`DC-bit-ui/Dylan-PM-`, branch `main`)
- Subagents that execute on this machine, not in a sandbox

The previous session was running in a Linux sandbox and could only act via paste-relay (Dylan copying outputs between surfaces). That's why the punch list below has manual steps — they were written for a constrained surface. **You don't have those constraints.** Where this handoff says "Dylan should run X in PowerShell," you should run it yourself via Bash.

**One coordination warning:** Cowork desktop is also pointed at `C:\Dylan PM` and may have a session open. If you write to a file Cowork is editing, expect conflicts. Check with Dylan before bulk operations.

---

## Goal

Get the Cowork OS from "infrastructure built, plumbing not flowing" to "in daily utilisation". The full punch list is in §"State of the punch list" below. **Critical path** is the next 2-3 actions; everything else can be sequenced from there.

---

## State of the punch list (as of 2026-04-29 SAST evening)

### Phase 0 — Bootstrap the seam ✅ COMPLETE (per Dylan, needs your verification)
- [x] 0.1 Clone repo into `C:\Dylan PM` — Dylan ran the PowerShell. **Verify:** `git log --oneline -5` should show `a8fc1b0` on top, with `7486a72` and `30fd1f8` directly below. Working tree should contain `CLAUDE.md`, `COWORK.md`, `memory/`, `.claude/`, `playbooks/`, `templates/`, `inbox/`, `workspace/`. If anything's missing, the clone didn't land — investigate.
- [x] 0.2 Re-run Cowork bootstrap probe in fresh thread — Dylan said done. **Open question:** did the probe actually load `CLAUDE.md` + `memory/profile/identity.md` + `memory/business/company.md`? Ask Dylan if not visible in `inbox/cowork/` or in Cowork chat history.
- [x] 0.3 Cowork VM git auth — **confirmed not available**. Punted to manual sync routine. VM capability probe drafted but deferred (see §"Drafted prompts" below).
- [x] 0.4 Teams connector disconnect/reconnect — done.

### Phase 1 — Apex flow consolidation 🟡 PENDING
- [ ] 1.1 Get a Daily Briefing output sample (today's 12:53 PM run, ideally). Either have Dylan paste it, or have Cowork drop it to `inbox/cowork/2026-04-29-daily-briefing-sample.md`.
- [ ] 1.2 Confirm: does Daily Briefing write to Notion or only chat? Where is its prompt actually coming from (instructions field is "TBD" but output is allegedly good)?
- [ ] 1.3 Decide flow consolidation. **Working hypothesis:** deprecate Morning Briefing + EOD Reconciliation, lean into Daily Briefing as primary. Validate against the sample.
- [ ] 1.4 Write proper instructions for Daily Briefing — light prompt, not heavy prescription. Codify what it does well based on the sample. Replace "TBD".
- [ ] 1.5 Update `memory/integrations/cowork.md` to reflect consolidated flow reality. CLAUDE.md §6 currently references stale "Morning Briefing 04:45 + EOD 12:00" — align that too.

### Phase 2 — Clear workstack noise 🟡 PENDING
- [ ] 2.1 Run stale-audit pass (prompt drafted in §"Drafted prompts" below). Cowork outputs to `inbox/cowork/2026-04-30-stale-audit.md`. Run after Phase 1 because Apex flow consolidation may change which Apex flow Dylan trusts.
- [ ] 2.2 Bulk-confirm cancellations in Notion based on audit (Dylan's hand on Notion).
- [ ] 2.3 Commit a decision-log entry capturing what was cancelled and why — `memory/decisions/2026-04-30-bulk-stale-cancel.md`.
- [ ] 2.4 Tighten the surviving Apex flow's stale-cleanup default: "cancel by default after 3d Proposed; resurrection requires explicit signal (Granola mention in 7d / Jira activity / Dylan starred)."

### Phase 3 — First real usage cycle 🟡 PENDING (this is "utilisation")
- [ ] 3.1 Run `/focus` first thing tomorrow morning — validate dual-stack output against actual day.
- [ ] 3.2 Run `/standup` — does it produce something Dylan would actually send?
- [ ] 3.3 Capture one Granola transcript through `/inbox-process` → routed to memory.
- [ ] 3.4 Log one decision via `/decision` to validate ADR flow.
- [ ] 3.5 EOD: run whichever Apex flow survived consolidation. File a real session retro via `/retro-day` (not a plumbing retro).

### Phase 4 — Refinement, after 1 week of real use 🟡 LATER
Schedule `/sweep` for next Friday. Don't pre-design — tune from observed reality.

---

## Decisions made this session (durable — don't relitigate)

1. **Stale-audit-then-confirm > bulk-cancel.** Original recommendation was "bulk-cancel everything >14d old". Revised: stale audit pass first, output candidate-cancel list with per-item reasoning, Dylan reviews, then bulk-confirm. Audit trail preserved.

2. **Apex flow noise comes from over-conservative cleanup logic, not just creation volume.** EOD Reconciliation's Step 3 says "if still relevant, keep" — too conservative. Tighten to "cancel by default after 3d; resurrection requires explicit signal". One instruction change > whole new flow.

3. **EOD schedule/instructions drift exists.** Schedule says 12:00 PM. Instructions reference 5:30 PM SAST. Today's actual run was 17:30 SAST. Recommendation: align both to 17:30 SAST (post-AEST-team-handoff window is genuinely useful). Pending implementation.

4. **Manual sync routine accepted as default.** Cowork VM has no git auth. Hands-off git would require setting up auth in the VM (capability probe drafted, deferred). For now: PowerShell `git pull` before Cowork session, `git add -A && git commit && git push` after if Cowork wrote anything. ~30s friction per session, clean audit trail.

5. **"Light prompt > heavy prompt" hypothesis worth capturing as a learning** if Daily Briefing's output validates it. Heavy prescription in Morning + EOD instructions may be over-constraining the model.

6. **Teams connector reliability is now a known risk.** 2 timeouts this week. Disconnect/reconnect attempted. If next 2-3 reconciliations also fail, it's not auth — investigate scoping or rate limits.

---

## Open questions (need Dylan's input or live diagnostic)

| Q | Why it matters | How to answer |
|---|---|---|
| Did the bootstrap probe actually read `CLAUDE.md`, `identity.md`, `company.md` after the clone? | Validates the seam is genuinely wired vs. just file-system-populated | Ask Dylan to paste probe results, or check `inbox/cowork/` |
| Where is Daily Briefing's prompt coming from given instructions = "TBD"? | Determines whether to "codify what works" or "stop trying to instruct it explicitly" | Inspect Cowork project settings; ask Dylan |
| Does Daily Briefing write to Notion or only chat? | Affects whether deprecating Morning is safe | Inspect a recent run; ask Dylan |
| Should EOD reschedule to 17:30 SAST? | Aligns instructions and schedule | Dylan's call |

---

## Drafted prompts (ready to dispatch)

### Stale audit pass — paste into a fresh Cowork thread when ready

```
APEX STALE AUDIT — One-off

Query Notion database fd5f23d7e071496dae6df273cbd901be for tasks where:
  Status IN ("Not started", "Proposed", "Queued")
  AND Due < (today - 14 days)

For each task, attach a "stale_reason" by checking:
  1. Linked Jira: closed/done/cancelled? → "Jira [AP-XXXX] is [status]"
  2. Granola: any mention of this task or its keywords in last 30 days? → "no recent meeting reference"
  3. Notion: duplicate or near-duplicate of a more recent task? → "duplicate of [task Y]"
  4. Date alone: nothing else, just stale by age → "no signal in 30+ days"

Output a single markdown table:
  | Task | Priority | Due | Days overdue | Stale reason | Recommended action |

Recommended action: "Cancel" (default for >30 days no signal), "Keep — recent signal", "Re-scope — context changed", "Resurrect — still relevant".

Write the table to inbox/cowork/2026-04-30-stale-audit.md.
DO NOT modify any Notion task statuses — Dylan will action manually after reviewing.
```

### VM capability probe — DEFERRED, only run if Dylan wants Cowork to do its own git later

```
COWORK VM CAPABILITY PROBE

Diagnose what your sandboxed VM can do regarding git operations.
Run each check, report results. DO NOT install software, write credentials,
or attempt actual push/clone — diagnostic only.

1. SHELL: What shell is available? (bash --version, or equivalent)
2. WORKING DIRECTORY: pwd
3. FOLDER MAPPING: What path does C:\Dylan PM map to inside your VM?
4. GIT INSTALLED: which git, git --version, exit code
5. GIT CONFIG: git config --global --list (keys only, no values)
6. PERSISTENT STORAGE: Are paths persistent between sessions? Test ~/.cowork-persistence-test
7. NETWORK: curl -I https://github.com 2>&1 | head -5
8. CREDENTIALS: presence (NOT values) of ~/.ssh/, ~/.netrc, $GH_TOKEN, $GITHUB_TOKEN

Output: markdown report with one section per check. End with one-line assessment:
"Git operations from this VM appear [feasible / infeasible / requires setup]" + rationale.
Write to inbox/cowork/<date>-vm-capability-probe.md.
```

---

## Recommended sequence for you (the local instance)

1. **Verify the bootstrap.** Run `git log --oneline -5`, list root, confirm match. Tell Dylan if anything's off.
2. **Ask Dylan for the Daily Briefing sample.** Without it, Phase 1 is blocked. Either have him paste, or have him ask Cowork to drop it to `inbox/cowork/`.
3. **Once you have the sample, do Phase 1.3-1.5 in one pass.** Decide consolidation, draft new instructions, update `cowork.md`. Commit each deliverable with clear messages.
4. **Dispatch the stale audit (Phase 2.1).** Print the prompt for Dylan to paste into Cowork. When the output lands in `inbox/cowork/`, route via `/inbox-process`.
5. **Phase 3 starts tomorrow morning** — that's Dylan's first real usage cycle. Be ready to support `/focus`, `/standup`, etc. Capture friction in `memory/learnings/`.
6. **Capture learnings as you go** per CLAUDE.md §10. The "light prompt > heavy prompt" insight is worth a `/log-learning` entry once the Daily Briefing sample confirms it.

---

## Active workspace context

- `inbox/cowork/` — should contain the bootstrap probe and any Cowork-authored captures. Process via `/inbox-process` periodically.
- `memory/learnings/2026-04/` — recent: `2026-04-29-cowork-bootstrap-probe.md`, `2026-04-29-mcp-surface-availability.md`. These ground the surface model.
- `memory/retros/session/2026-04-29-eod.md` — Apex EOD output preserved with annotation. Long-term should move to `memory/integrations/cowork/eod-reconciliation/` — refactor pending Dylan's call.

---

## What NOT to do

- **Don't scaffold a fresh memory tree.** Cowork tried this when it couldn't see the canonical state. The tree is fully populated; trust it.
- **Don't push to a branch other than `main`.** Precedent established this session. Dylan's branch directive system says `claude/setup-claude-system-9cDDB` but operationally main has been the working branch and Dylan hasn't pushed back.
- **Don't auto-write to Notion / Jira / Teams / Outlook from this repo.** Per CLAUDE.md §12. Apex handles routine writes; this repo is read-primary.
- **Don't relitigate the decisions in the section above.** They're durable.

---

## Closing instruction

Once you've done items 1-2 above (verify bootstrap + ask for Daily Briefing sample), supersede this handoff with a forward link rather than deleting it. The handoff itself is the durable record of the bootstrap moment.

When you're done, file a session retro at `memory/retros/session/2026-04-29-cowork-os-handoff-completed.md` (or 2026-04-30, whenever you finish) summarising what landed.

End of handoff.

---

## Supersede log

**2026-04-29 (later same day) — Phase 1 hypothesis falsified, Phase 1 outcomes superseded.**

This handoff's working hypothesis ("deprecate Morning 